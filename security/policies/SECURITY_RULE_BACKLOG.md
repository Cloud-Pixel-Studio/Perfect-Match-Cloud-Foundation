# Security Rule Backlog

Future validated rules should cover tenant-boundary bypass, unsafe raw SQL,
hardcoded secrets, authorization disabling, unsafe token validation, dangerous
deserialization, and security-bypass comments or configuration. PMC-00 includes
only the tested wildcard-CORS rule foundation.
